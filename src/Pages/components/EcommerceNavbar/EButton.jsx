import React from 'react'

const EButton = ({name}) => {
  return (
        <button className='bg-blue-400 py-2 px-4  rounded-full text-white text-[1rem] font-semibold '>
            {name}
        </button>
  )
}

export default EButton